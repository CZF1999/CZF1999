# 大屏自适应方案

---

## 一、问题背景

### 原始架构

页面使用 CSS Grid 布局（`ScreenLayout` 组件），通过 `fr` 单位和百分比宽度将面板划分到网格中。这种方案让**面板边框**在不同分辨率下保持了正确的位置和比例，但**内部内容**——字体、图标、ECharts 图表中的文字标签、线条粗细、散点大小等——全部使用了固定的像素值（`px`）。

### 问题现象

| 分辨率 | 面板位置 | 内部字体 | 图表标签 | 效果 |
|--------|:---:|:---:|:---:|------|
| 1920×1080（设计稿） | ✅ | ✅ | ✅ | 正常 |
| 3840×2160（4K） | ✅ | ❌ 偏小 | ❌ 偏小 | 图表文字挤在一起 |
| 1366×768（小屏） | ✅ | ❌ 偏大 | ❌ 溢出 | 文字重叠、标签截断 |
| 3840×1080（超宽屏） | ✅ | ❌ 偏小 | ❌ 偏小 | 只有中间一条内容 |

根本原因：**布局层的 `fr`/`%` 自适应了，内容层的 `px` 没有**。

### 备选方案对比

| 方案 | 布局自适应 | 内容自适应 | 点击坐标正确 | 三方组件兼容 | 维护成本 |
|------|:---:|:---:|:---:|:---:|:---:|
| 纯 Grid + px（旧方案） | ✅ | ❌ | ✅ | ✅ | 低 |
| `transform: scale()` | ✅ | ✅ | ❌ | ❌ | 中 |
| 纯 CSS `vw` 单位 | ✅ | ⚠️ 仅宽度 | ✅ | ✅ | 低 |
| **rem + JS 动态根字体** | ✅ | ✅ | ✅ | ✅ | 低 |

- **`transform: scale()`** 缩放的是渲染结果，浏览器认为元素还在原位，导致鼠标点击坐标偏移、弹出层/下拉菜单定位错乱、滚动行为异常
- **`vw`** 只跟随视口宽度变化，超宽屏（3840×1080）下内容会纵向溢出，竖屏下横向溢出
- **`rem` + JS** 无上述问题，同时考虑宽高、取较小者，是大屏项目的成熟方案

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        DashboardView.vue                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    ScreenLayout (Grid)                     │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │  │
│  │  │TrendChart│  │GaugeChart│  │ PieChart │  │Device3DView│ │  │
│  │  │ (EChart) │  │ (EChart) │  │ (EChart) │  │ (Three.js)│ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │  │
│  │  │ BarChart │  │ PieChart │  │AlarmList │  │ MapView  │  │  │
│  │  │ (EChart) │  │ (EChart) │  │  (DOM)   │  │ (EChart) │  │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

两层缩放体系：

  布局层（CSS Grid fr/%）
  ├── ScreenLayout: grid-template-columns/rows 使用 fr 单位
  ├── 各面板: grid-column/row 使用 span 分配比例
  ├── ChartBox: 100% × 100% 填满 grid cell
  └── EChart/Three.js 容器: 100% × 100% 填满 ChartBox body

  内容层（rem + scale）
  ├── useScreenScale.ts: 动态设置 html font-size
  ├── dashboard.less: 所有 px → rem（字体、间距、边框、圆角）
  ├── ECharts option: fontSize * scale、*Width * scale、symbolSize * scale
  ├── AlarmList: ITEM_HEIGHT = computed(() => 36 * scale)
  └── ScreenLayout: gap = px/16 rem
```

---

## 三、核心模块详解

### 3.1 useScreenScale.ts —— 全局缩放单例

**文件**：`src/utils/useScreenScale.ts`

```ts
import { ref } from 'vue'

const DESIGN_WIDTH = 1920
const DESIGN_HEIGHT = 1080
const BASE_FONT_SIZE = 16   // 设计稿 1rem = 16px

const scale = ref(1)
let initialized = false

function updateScale() {
  const wRatio = window.innerWidth / DESIGN_WIDTH
  const hRatio = window.innerHeight / DESIGN_HEIGHT
  // 取宽高缩放比的较小者 —— 保证内容始终完整可见
  scale.value = Math.min(wRatio, hRatio)
  document.documentElement.style.fontSize = `${BASE_FONT_SIZE * scale.value}px`
}

export function useScreenScale() {
  if (!initialized) {
    initialized = true
    updateScale()
    window.addEventListener('resize', updateScale)
  }
  return { scale }
}
```

#### 设计要点

1. **模块级单例**：`scale` ref 和 `initialized` 标志定义在模块作用域，不在 `useScreenScale()` 函数内。无论多少组件调用 `useScreenScale()`，返回的都是同一个 `scale` ref，`resize` 监听器全局只注册一次。

2. **双向考虑**：`Math.min(wRatio, hRatio)` 取宽高比的较小值。这保证了：
   - 超宽屏（3840×1080）：高度比是 1.0，scale=1.0，内容不溢出，左右自然留白
   - 竖屏（1080×1920）：宽度比是 0.56，scale=0.56，内容不溢出，上下自然留白

3. **响应式驱动**：`scale` 是 Vue `ref`，`scale.value` 变化会触发所有依赖它的 `computed` 重新计算，ECharts 通过 `watch` 自动更新 option。这是方案能**实时跟随窗口 resize** 的关键。

4. **rem 联动**：`document.documentElement.style.fontSize` 被直接写入，CSS 中所有 `rem` 单位自动跟随，无需 JS 逐个操作 DOM。

### 3.2 EChart.vue —— ECharts 容器的 ResizeObserver 机制

**文件**：`src/components/EChart.vue`

这是所有 ECharts 图表的底层容器，负责实例创建、销毁和尺寸跟随。

```
┌─────────────────────────────────────────────────────┐
│                    EChart.vue                        │
│                                                      │
│  onMounted                                           │
│  ├── echarts.init(container, theme, {               │
│  │     devicePixelRatio: getOptimalDPR(2)           │
│  │   })                                              │
│  ├── chart.setOption(props.option)                  │
│  ├── new ResizeObserver(() => scheduleResize())     │
│  │     .observe(container)                          │
│  └── emit('ready', chartInstance)                   │
│                                                      │
│  watch(() => props.option, (opt) => {               │
│    chart.setOption(opt)                              │
│    nextTick(() => doResize())  // 防止 display:none  │
│  })                                                  │
│                                                      │
│  onBeforeUnmount                                     │
│  ├── cancelAnimationFrame(rafId)                    │
│  ├── resizeObserver.disconnect()                    │
│  └── chart.dispose()                                │
└─────────────────────────────────────────────────────┘
```

#### ResizeObserver 的作用

```ts
// 1. 尺寸变化检测
resizeObserver = new ResizeObserver(() => {
  scheduleResize()
})
resizeObserver.observe(containerRef.value)
```

`ResizeObserver` 监听的是 **DOM 元素的 content-box 尺寸变化**，不是 `window.resize`。这意味着它响应的是容器自身的尺寸变化——无论是：
- 浏览器窗口 resize 导致 Grid 重算 `fr` 后 cell 尺寸变了
- CSS 变化导致容器尺寸变了
- 父组件条件渲染导致容器出现了

#### 防抖策略

```ts
// 2. requestAnimationFrame 节流
function scheduleResize(): void {
  if (rafId !== null) return          // 同一帧内多次触发只执行一次
  rafId = requestAnimationFrame(() => {
    rafId = null
    doResize()
  })
}

// 3. 去重 —— 尺寸没变就不调 chart.resize()
function doResize(): void {
  const w = el.clientWidth
  const h = el.clientHeight
  if (lastSize && lastSize.w === w && lastSize.h === h) return
  lastSize = { w, h }
  chart?.resize({ width: w, height: h })
}
```

两层优化：
- `scheduleResize`：RAF 节流，同一帧内 ResizeObserver 多次回调合并为一次
- `doResize`：尺寸去重，尺寸完全不变时跳过 `chart.resize()`

#### option watch 与 display:none 盲区

```ts
watch(() => props.option, (opt) => {
  chart.setOption(opt, { notMerge: false })
  nextTick(() => {
    // 场景：tab 切换时容器被 display:none 隐藏再显示
    // ResizeObserver 在某些浏览器中的 display:none → block
    // 切换时可能不触发回调
    // 所以用 nextTick 确保 DOM 更新后再检查一次尺寸
    const el = containerRef.value
    if (el && el.clientWidth > 0 && el.clientHeight > 0) {
      if (!lastSize || lastSize.w !== el.clientWidth || lastSize.h !== el.clientHeight) {
        doResize()
      }
    }
  })
})
```

#### DPR 封顶

```ts
// src/utils/dashboard.ts
export function getOptimalDPR(maxDPR = 2): number {
  return Math.min(window.devicePixelRatio, maxDPR)
}
```

初始化 ECharts 时 `devicePixelRatio` 封顶为 2。这样在 4K 屏幕（DPR=2）和 8K 屏幕（DPR=2）下，ECharts 内部的 canvas 像素数受控——实际渲染像素 = CSS 像素 × min(设备DPR, 2)，不会随分辨率平方级增长。

---

### 3.3 缩放传递路径：scale → ECharts option

以 `TrendChart.vue` 为例，完整的缩放传递链路如下：

```
window.resize
  │
  ▼
useScreenScale.updateScale()
  ├── scale.value = Math.min(wRatio, hRatio)    // Vue ref 更新
  └── document.documentElement.style.fontSize   // CSS rem 更新
        │
        ├──────────────────────────────────────┐
        ▼                                      ▼
  dashboard.less 中的 rem 自动生效        ECharts option computed 重新计算
  (字体、间距、边框、圆角)                   │
                                             ▼
                                      baseOption = computed(() => {
                                        const s = scale.value
                                        return {
                                          grid: { left: 50 * s },
                                          tooltip: { fontSize: 12 * s },
                                          xAxis: { fontSize: 10 * s },
                                          yAxis: { fontSize: 10 * s },
                                          dataZoom: { height: 20 * s },
                                        }
                                      })
                                             │
                                    watch 触发 setOption
                                             │
                                             ▼
                                    EChart.vue chart.setOption(newOption)
                                             │
                                    nextTick → doResize()
                                             │
                                             ▼
                                    chart.resize({ width, height })
```

注意这个链路中的两个 resize 维度：
1. **内容缩放**：`baseOption` 是 `computed`，依赖 `scale.value`，scale 变化 → option 字号/尺寸重新计算 → `watch` 触发 `chart.setOption()` → ECharts 内部重绘文字大小
2. **容器缩放**：Grid cell 尺寸变化 → `ResizeObserver` 回调 → `chart.resize()` → ECharts 内部重算布局

两者独立但协同：`setOption` 控制**画什么、画多大字号**，`resize` 控制**画布本身的像素尺寸**。

---

### 3.4 各图表组件的缩放处理

#### TrendChart（趋势线图）

```ts
const baseOption = computed(() => {
  const s = scale.value
  return {
    grid: { left: 50 * s, right: 20 * s, top: 10 * s, bottom: 50 * s },
    tooltip: { textStyle: { fontSize: 12 * s } },
    xAxis: { axisLabel: { fontSize: 10 * s } },
    yAxis: { axisLabel: { fontSize: 10 * s } },
    dataZoom: [
      { type: 'inside', start: 0, end: 100 },
      { type: 'slider', height: 20 * s, bottom: 6 * s },
    ],
    // series lineStyle width 保持 2px 不缩放 —— 线条粗细在设计上不要缩放
  }
})
```

#### BarChart（柱状图）

```ts
const chartOption = computed(() => {
  const s = scale.value
  return {
    grid: { left: 60 * s, right: 20 * s, top: 10 * s, bottom: 30 * s },
    tooltip: { textStyle: { fontSize: 12 * s } },
    xAxis: { axisLabel: { fontSize: 10 * s } },
    yAxis: { axisLabel: { fontSize: 10 * s } },
    series: [{ barMaxWidth: 40 * s }],
  }
})
```

#### GaugeChart（仪表盘）

GaugeChart 的缩放对象最丰富，几乎所有有 px 值的配置项都需要缩放：

```ts
const chartOption = computed(() => {
  const s = scale.value
  return {
    series: [{
      axisLine: { lineStyle: { width: 14 * s } },
      pointer: { width: 6 * s },
      axisTick: { distance: -(14 * s), length: 6 * s },
      splitLine: { distance: -(20 * s), length: 12 * s },
      axisLabel: { distance: 28 * s, fontSize: 10 * s },
      anchor: { size: 14 * s },
      detail: { fontSize: 28 * s },
    }],
  }
})
```

注意 `distance` 是**负值**，缩放时保留符号：`-(14 * s)`。

#### PieChart（饼图）

三种维度的饼图（设备状态、告警级别、设备类型）处理方式一致：

```ts
function buildStatusOption() {  // 同理 buildAlarmLevelOption / buildDeviceTypeOption
  const s = scale.value
  return {
    tooltip: { textStyle: { fontSize: 12 * s } },
    legend: { textStyle: { fontSize: 10 * s } },
    series: [{
      radius: ['40%', '70%'],   // 百分比不变
      center: ['50%', '45%'],    // 百分比不变
      emphasis: { label: { fontSize: 14 * s } },
    }],
  }
}
```

`radius`、`center` 使用 `%`，它们是相对于容器尺寸的比例，不需要缩放。

#### MapView（设备分布散点图）

```ts
const chartOption = computed(() => {
  const s = scale.value
  return {
    xAxis: { axisLabel: { fontSize: 9 * s } },
    yAxis: { axisLabel: { fontSize: 9 * s } },
    tooltip: { textStyle: { fontSize: 12 * s } },
    series: [{
      type: 'effectScatter',
      symbolSize: 12 * s,        // 散点大小跟随缩放
    }],
  }
})
```

#### AlarmList（告警列表 —— 纯 DOM 组件）

```ts
const { scale } = useScreenScale()
const BASE_ITEM_HEIGHT = 36
const ITEM_HEIGHT = computed(() => BASE_ITEM_HEIGHT * scale.value)

function onScroll(): void {
  visibleStart.value = Math.floor(scrollTop / ITEM_HEIGHT.value)
  visibleCount.value = Math.ceil(scrollRef.value.clientHeight / ITEM_HEIGHT.value) + 2
}
```

AlarmList 的虚拟滚动依赖 `ITEM_HEIGHT` 计算可见范围。改为 `computed` 后，scale 变化时 `ITEM_HEIGHT` 自动更新，下一次 `onscroll` 事件就会用新的行高计算。

### 3.5 Device3DView —— Three.js 的自适应

**文件**：`src/views/dashboard/components/Device3DView.vue`

Three.js 的 3D 场景没有"字号"问题，它的自适应完全通过 `ResizeObserver` 处理：

```ts
// 初始化
function initScene(): void {
  const w = threeContainer.value.clientWidth
  const h = threeContainer.value.clientHeight
  renderer.setSize(w, h, false)
  camera = new THREE.PerspectiveCamera(45, w / h, 0.1, 1000)
  // ...
}

// ResizeObserver 监听容器尺寸
resizeObserver = new ResizeObserver(() => scheduleResize())
resizeObserver.observe(threeContainer.value)

function handleResize(): void {
  const w = threeContainer.value.clientWidth
  const h = threeContainer.value.clientHeight
  renderer.setSize(w, h, false)
  camera.aspect = w / h
  camera.updateProjectionMatrix()
}
```

缩放只影响布局（Grid cell 大小），Three.js 内部模型几何体不变，这也是合理的——3D 模型本身没有"字号大小"概念。

### 3.6 CSS 层：dashboard.less 的 rem 化

**文件**：`src/styles/dashboard.less`

所有固定 `px` 值转换为 `rem`，换算公式：`rem = px / 16`。

```less
// 字体
@font-xs: 0.75rem;     // 12px ÷ 16
@font-sm: 0.875rem;    // 14px ÷ 16
@font-md: 1rem;        // 16px ÷ 16
@font-lg: 1.25rem;     // 20px ÷ 16
@font-xl: 1.75rem;     // 28px ÷ 16
@font-xxl: 2.25rem;    // 36px ÷ 16

// 间距
@gap-xs: 0.25rem;      // 4px
@gap-sm: 0.5rem;       // 8px
@gap-md: 0.75rem;      // 12px
@gap-lg: 1rem;         // 16px
@gap-xl: 1.5rem;       // 24px

// 边框
@border-radius: 0.25rem;    // 4px
@border-width: 0.0625rem;   // 1px

// 组件样式中的 px 也要换
.alarm-list__scroll::-webkit-scrollbar { width: 0.25rem; }    // 4px
.alarm-list__item--info { border-left: 0.1875rem solid ...; }  // 3px
.metric-card__unit { margin-left: 0.125rem; }                  // 2px
```

### 3.7 ScreenLayout —— Grid gap 的 rem 化

**文件**：`src/components/ScreenLayout.vue`

```ts
// gap 输入为设计像素，内部自动转为 rem
gap: `${props.gap / 16}rem`
```

DashboardView 使用 `:gap="20"`，即设计稿上 20px 的间距。内部转为 `1.25rem`，跟随 `html font-size` 等比缩放。

---

## 四、各分辨率表现

### 设计基准：1920×1080

| 屏幕 | 常见分辨率 | wRatio | hRatio | scale | 内容比例 | 布局 |
|------|-----------|--------|--------|-------|---------|------|
| 设计稿 | 1920×1080 | 1.00 | 1.00 | 1.00 | 原始大小 | 满屏 |
| 小屏 | 1366×768 | 0.71 | 0.71 | 0.71 | 缩小 29% | 满屏 |
| 2K QHD | 2560×1440 | 1.33 | 1.33 | 1.33 | 放大 33% | 满屏 |
| 4K UHD | 3840×2160 | 2.00 | 2.00 | 2.00 | 放大 100% | 满屏 |
| 8K | 7680×4320 | 4.00 | 4.00 | 4.00 | 放大 300% | 满屏 |
| 超宽屏 | 3840×1080 | 2.00 | 1.00 | **1.00** | 原始大小 | **左右留白** |
| 竖屏 | 1080×1920 | 0.56 | 1.78 | **0.56** | 缩小 44% | **上下留白** |

### 关键场景解释

**标准 16:9 屏幕**（1366×768, 1920×1080, 2560×1440, 3840×2160, 7680×4320）：
wRatio ≈ hRatio，scale 就是宽高比，内容等比放大到填充整个屏幕，设计稿比例完美保留。

**超宽屏**（3840×1080 等 32:9 屏幕）：
宽度是设计稿的 200%，但高度只有 100%。取 `min` 后 scale=1.0，内容保持设计稿大小。Grid 的 `fr` 单位填满宽度（面板变宽），内容大小不变，左右自然留白但面板充分利用空间。

**竖屏**（1080×1920）：
高度是设计稿的 178%，宽度只有 56%。取 `min` 后 scale=0.56，内容等比缩小到 56%。Grid 填满高度（面板变高），内容等比缩小不溢出。

---

## 五、DPR 与渲染性能

```ts
// src/utils/dashboard.ts
export function getOptimalDPR(maxDPR = 2): number {
  return Math.min(window.devicePixelRatio, maxDPR)
}
```

### 设备 DPR 与最终渲染像素

| 设备 | 分辨率 | 系统缩放 | innerWidth | DPR | getOptimalDPR | ECharts canvas 像素 |
|------|--------|---------|------------|-----|---------------|---------------------|
| 标准显示器 | 1920×1080 | 100% | 1920×1080 | 1 | 1 | 约 400×300 |
| Mac Retina | 2560×1600 | 200% | 1280×800 | 2 | 2 | 约 800×600 |
| 4K 显示器 | 3840×2160 | 150% | 2560×1440 | 2 | 2 | 约 1200×900 |
| 4K 显示器 | 3840×2160 | 200% | 1920×1080 | 2 | 2 | 约 600×450 |
| 8K 显示器 | 7680×4320 | 200% | 3840×2160 | 2 | 2 | 与 4K 基本持平 |
| 8K 显示器 | 7680×4320 | 300% | 2560×1440 | 3 | **2** | 封顶到 2 倍 |

### 性能分析

- `scale` 控制的缩放是 **CSS 层面的**，不增加 canvas 渲染像素数
- `devicePixelRatio` 控制的才是 **canvas 内部像素数**
- DPR 封顶为 2，8K 下 canvas 像素数不会比 4K 多（都是 2× CSS 像素）
- 真正的渲染瓶颈是 **WebSocket 数据刷新频率** 和**数据量**，不是分辨率

---

## 六、文件变更清单

### 新增文件

| 文件 | 说明 |
|------|------|
| `src/utils/useScreenScale.ts` | 核心 composable，模块级单例，计算 scale 并设置 html font-size |

### 修改文件

| 文件 | 改动类型 | 改动内容 |
|------|---------|---------|
| `src/styles/dashboard.less` | px→rem | 全部字体、间距、边框、圆角、滚动条宽度转换为 rem |
| `src/components/ScreenLayout.vue` | vw→rem | gap 从 `vw` 改为 `设计px/16 rem`，跟随 html font-size 缩放 |
| `src/views/dashboard/DashboardView.vue` | 初始化 | 引入 `useScreenScale()` 初始化全局缩放单例 |
| `src/views/dashboard/components/TrendChart.vue` | 动态缩放 | grid fontSize sliderHeight × scale |
| `src/views/dashboard/components/BarChart.vue` | 动态缩放 | grid fontSize barMaxWidth × scale |
| `src/views/dashboard/components/GaugeChart.vue` | 动态缩放 | lineWidth pointer splitLine axisLabel detail anchor × scale |
| `src/views/dashboard/components/PieChart.vue` | 动态缩放 | 三种维度 tooltip legend emphasis fontSize × scale |
| `src/views/dashboard/components/MapView.vue` | 动态缩放 | axisLabel tooltip fontSize symbolSize × scale |
| `src/views/dashboard/components/AlarmList.vue` | 动态缩放 | ITEM_HEIGHT 从常量 36 改为 computed(() => 36 * scale) |

### 不需要修改的文件

| 文件 | 原因 |
|------|------|
| `src/components/EChart.vue` | 封装层不涉及字号值，scaled option 通过 `watch` + `setOption` 自动生效；`ResizeObserver` + `chart.resize()` 处理容器尺寸变化 |
| `src/components/ChartBox.vue` | 纯 CSS 布局组件，100% 填满 Grid cell，内部间距通过 `dashboard.less` 的 rem 变量自动跟随 |
| `src/views/dashboard/components/Device3DView.vue` | Three.js 走 `ResizeObserver` 自适应容器尺寸，3D 场景无"字号大小"概念 |

---

## 七、扩展指南

### 新增图表组件的规则

1. **CSS 尺寸**：统一用 `rem`，换算公式 `rem = 设计px / 16`
   ```less
   .my-component { font-size: 0.875rem; padding: 0.5rem; }
   ```

2. **ECharts option 中的 px 值**：`const s = scale.value`，然后 `数值 * s`
   ```ts
   const { scale } = useScreenScale()
   const option = computed(() => {
     const s = scale.value
     return {
       xAxis: { axisLabel: { fontSize: 10 * s } },
       grid: { left: 50 * s },
     }
   })
   ```

3. **ECharts option 中的百分比/字符串值**：保持不变
   ```ts
   // 不需要缩放
   center: ['50%', '45%']
   radius: '85%'
   ```

4. **JS 中的计算尺寸**：通过 `scale.value` 手动计算
   ```ts
   const LINE_HEIGHT = computed(() => 36 * scale.value)
   ```

5. **Three.js / Canvas 2D**：使用 `ResizeObserver` 监听容器尺寸，用 `container.clientWidth/Height` 设置画布大小，内部绘制坐标不变（3D 场景不需要"字号缩放"）

### screenScale 单例的使用方式

由于是模块级单例，任何组件中 `useScreenScale()` 返回的都是同一个 `scale` ref：

```ts
// 组件 A
const { scale } = useScreenScale()  // 同一个 ref
// 组件 B
const { scale } = useScreenScale()  // 同一个 ref
```

这意味着 scale 变化时，所有使用了 `scale.value` 的 `computed` 都会同步更新。

### 不需要再次初始化

`useScreenScale()` 首次调用时自动初始化（设置 html font-size + 注册 resize 监听器），后续调用直接返回已有 `scale` 引用。DashboardView 中的调用就是首次初始化，其他图表组件中的调用都是引用。

---

## 八、注意事项

1. **ResizeObserver 的 display:none 盲区**：EChart.vue 在 `option` watch 中用 `nextTick` + 手动 `doResize()` 兜底，防止 tab 切换导致的尺寸未更新

2. **ECharts option 的计算依赖**：所有需要缩放的值必须放在 `computed` 内部、在 `const s = scale.value` 之后，确保每次 scale 变化时都重新计算

3. **RAF 节流**：`EChart.vue` 和 `TrendChart.vue` 都使用了 `requestAnimationFrame` 合并高频更新（ResizeObserver 回调、WebSocket 数据推送），这是一个重要的性能优化模式

4. **负值缩放**：`GaugeChart` 的 `axisTick.distance` 和 `splitLine.distance` 是负值，缩放时保留符号：`-(14 * s)`

5. **rem 是全局的**：`document.documentElement.style.fontSize` 会影响整个页面。如果 dashboard 之外还有其他页面，它们也会受影响。因为 `dashboard-fullscreen` class 只在 dashboard 页面添加，且 `html` 的 `font-size` 只在页面加载时设置、卸载时不会清除（因为是单例），在实际使用中（dashboard 就是全屏的主页面）这是可以接受的
